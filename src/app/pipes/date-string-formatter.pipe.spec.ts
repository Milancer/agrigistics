import { DateStringFormatterPipe } from './date-string-formatter.pipe';

describe('DateStringFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new DateStringFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
